"""One-question harnesses: `diag_*` measure a mechanism, `run_*` measure a
change. Run from the repo root as `python -m diagnostics.<name>`.

Each one exists because a claim needed evidence, and each has its findings
written up in `KNOWN_ISSUES.md` under a numbered section. A harness whose
output nobody wrote up is not finished work.
"""
