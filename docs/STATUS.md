# Status

GENERATED by `python -m tools.status --write`. **Do not edit by hand.**

Derived at `5dc60f5`. Every figure below is read off the artefact that
produced it — the table headers in `results/`, the fingerprints in
`tools/cache_manifest`, the ledger in `edhmc/pending`, the index in
`KNOWN_ISSUES.md`, the modules on disk. **Nothing here is remembered,**
so nothing here can be stale without the thing it describes having moved.

**What this file cannot tell you.** Whether `validate.py` prints `+0.00`
today, whether the tests pass, whether a number is *right*. Freshness of
a check is not derivable from the filesystem, and a cheerful "all checks
pass" that nothing verified is the exact failure this project keeps
finding in its own documentation. Run the commands in **Before you trust
a number** below.

## The six ablation tables

`rows` counts the printed card rows, split by category. A MODEL-BLIND
row is **not measured**, never a cut candidate.

| deck | table | N | horizons | noise floor | rows (eval/partial/blind) | last commit |
|---|---|---|---|---|---|---|
| rendmaw | `ablation_rendmaw.txt` | 15,000 | 10, 20 | ±0.0019 | 40/3/21 = 64 | 2026-09-16 |
| lorehold | `ablation_lorehold.txt` | 15,000 | 10, 20 | ±0.0033 | 38/7/20 = 65 | 2026-09-16 |
| karlov | `ablation_karlov.txt` | 15,000 | 10, 20 | ±0.0025 | 44/5/15 = 64 | 2026-09-16 |
| tivit | `ablation_tivit.txt` | 15,000 | 10, 20 | ±0.0027 | 41/4/19 = 64 | 2026-09-16 |
| shilgengar | `ablation_shilgengar.txt` | 15,000 | 10, 20 | ±0.0020 | 42/2/20 = 64 | 2026-09-16 |
| azusa | `ablation_azusa.txt` | 15,000 | 10, 20 | ±0.0030 | 43/1/14 = 58 | 2026-09-15 |

**379 rows across six tables.**

## Ablation caches

`ablation.py` keys its cache on deck, horizons, N and blank mode — **not
on the code that produced it** — so a stale cache makes a run reprint old
numbers instead of measuring. The fingerprint is the guard;
`docs/ABLATION_CACHES.md` records it.

**6 cache files on disk; 6 recorded in the manifest.**

Per-deck state. **A moved fingerprint is SUSPECT, not condemned** —
resolve it with `tools/check_unchanged_decks.py` against a worktree at
the cache's `built_commit` (seconds), then record it with
`python -m tools.cache_manifest --verified <deck> "<evidence>"`.
§0z23 for why the old "delete it" rule was the defect.

| deck | built at | live | state | caches |
|---|---|---|---|---|
| rendmaw | `0ac5ad66a8f9fff1` | `7d2f689ebae32022` | **VERIFIED** | 1 |
| lorehold | `fbc552aea5d15b19` | `58e9ba5655aadfa1` | **VERIFIED** | 1 |
| karlov | `431e2b18fdea7711` | `1a239ad6e2dee0ae` | **VERIFIED** | 1 |
| tivit | `d55ddbf4fa5ff163` | `643f4cbb32e268df` | **VERIFIED** | 1 |
| shilgengar | `9f1e2c62af6cd20b` | `438c94510e98b2c9` | **VERIFIED** | 1 |
| azusa | `9574b38be817712d` | `87f0080a94f5a10f` | **VERIFIED** | 1 |

## The ledger

Straight out of `edhmc/pending.py`, which is the only trustworthy
statement of what is staged. Run `python -m edhmc.pending` for the
evidence behind each entry.

| deck | committed | staged | measured | withdrawn |
|---|---|---|---|---|
| rendmaw | 1 | 0 | 0 | 1 |
| lorehold | 4 | 2 | 0 | 0 |
| karlov | 3 | 1 | 0 | 0 |
| tivit | 0 | 0 | 0 | 0 |
| shilgengar | 0 | 0 | 0 | 0 |
| azusa | 4 | 1 | 15 | 0 |
| **all** | **12** | **4** | **15** | **1** |

### Staged and uncommitted

| deck | out | in | staged |
|---|---|---|---|
| karlov | Swamp | Bolas's Citadel | 2026-09-12 |
| azusa | Perilous Forays | Ka-Zar of the Savage Land | 2026-09-10 |
| lorehold | Penance | Caldera Pyremaw | 2026-09-05 |
| lorehold | Scroll Rack | Sunbird's Invocation | 2026-09-04 |

### Withdrawn

Staged and then unstaged. **Not a refutation** — the entry is kept
so the same card is not re-measured from scratch.

| deck | out | in | withdrawn |
|---|---|---|---|
| rendmaw | Idol of Oblivion | Cauldron of Essence | UNSTAGED 2026-09-13. NOT REFUTED AND NOT REJECTED — unstaged because the decisio |

## Open findings

From `KNOWN_ISSUES.md`'s index. **The section ids are load-bearing** —
they are cited from code — so they are never renumbered.

**CLOSED** 7 · **FIXED** 33 · **MEASURED** 14 · **OPEN** 2 · **PARTLY RESOLVED** 1 · **RESOLVED** 3 · **superseded** 1

Still live:

- **§0z24** — **`FLIP` is assigned on an unguarded sign and overrides `--`** — 11 of 22 FLIP rows across six tables would read "unmeasured" on their own merits
- **§4** — opponents' boards are a blocker count — the project's oldest limit

## What can be run

Discovered from disk, so this list cannot drift from the modules that
exist — which the hand-written lists it replaces had already done, in
seven places. **Everything runs from the repo root with `-m`.**

### `tools/` — 14

| command | what it does |
|---|---|
| `python -m tools.ablation` |  |
| `python -m tools.audit_cards` | Re-verify every card in every deck module against Scryfall oracle text. |
| `python -m tools.build_tivit_xlsx` | Build Tivit_Seller_of_Secrets_Commander_Deck_v1.xlsx. |
| `python -m tools.cache_manifest` | Record what code each committed ablation cache was produced by. |
| `python -m tools.candidates` |  |
| `python -m tools.check_docs` | Fail when the documentation stops describing the repository. |
| `python -m tools.check_unchanged_decks` | Did a shared-code change move a deck it was not supposed to move? |
| `python -m tools.compare_decks` |  |
| `python -m tools.fit_pod` | Fit the pod model so that COMBAT, not the clock, does most of the killing. |
| `python -m tools.knobs` | Derive the registry of simulation knobs from the engines that read them. |
| `python -m tools.status` | Derive the project's CURRENT STATE from the repo, instead of remembering it. |
| `python -m tools.tag_flying` | Fetch FLYING and INDESTRUCTIBLE for every card in the four decks, from Scryfall. |
| `python -m tools.tutor_policy` |  |
| `python -m tools.validate` | Harness validation: A/A control + measurement of the CRN variance reduction. |

### `tests/` — 13

| command | what it does |
|---|---|
| `python -m tests.test_ashaya` | §0z18 / queued 15 — Ashaya's second clause, one assertion per rule. |
| `python -m tests.test_azusa_batch3` | Pin the mechanisms of the 2026-09-10 Azusa candidates (batch 3). |
| `python -m tests.test_azusa_batch4` | Pin the mechanisms of the 2026-09-13 Azusa candidates (batch 4). |
| `python -m tests.test_azusa_candidates` | Pin the mechanisms of the 2026-09-09 Azusa candidates. |
| `python -m tests.test_combat_split` | Pins `opponents.combat_damage`'s assignment rule — the one that splits an |
| `python -m tests.test_crn_streams` | §0z17 — the mid-game randomness is addressed, not ordered. |
| `python -m tests.test_lorehold_0f_0i` | Pin the two cheap closures from the KNOWN_ISSUES review: §0i and §0f. |
| `python -m tests.test_mana_colour` | Pin colour-correct payment: the engine taps what it proved it could tap. |
| `python -m tests.test_modes_and_altar` | §1b and §3 — castable modes, and Ashnod's Altar's mana. |
| `python -m tests.test_pod_damage_and_wipes` | Pin three rules that six engines implement and nothing checked agreed. |
| `python -m tests.test_recursion` | §7 / §0z19 — the three Lorehold recursion cards, one assertion per clause. |
| `python -m tests.test_time_sieve` | The Tivit + Time Sieve turn loop, pinned as a test. |
| `python -m tests.test_tivit_combo` | The Deadeye loop's mana economy, pinned as a test. |

### `diagnostics/` — 27

| command | what it does |
|---|---|
| `python -m diagnostics.diag_azusa_animation` | The land-animation pillar was untested, not disproved. Four fixes, measured. |
| `python -m diagnostics.diag_azusa_batch3` | What do the 2026-09-10 Azusa candidates actually DO, per game? |
| `python -m diagnostics.diag_azusa_batch4` | What do the 2026-09-13 Azusa candidates actually DO, per game? |
| `python -m diagnostics.diag_azusa_lands` | Does the Azusa engine sequence its land drops the way a pilot would? |
| `python -m diagnostics.diag_fivedrop` | Why each Penance replacement scores what it scores. |
| `python -m diagnostics.diag_shilgengar_ult` | Shilgengar's own ability has never fired. Why, and what it is worth. |
| `python -m diagnostics.diag_threat_blank` | Is a card's ablation score measuring its TEXT, or its THREAT NUMBER? |
| `python -m diagnostics.diag_time_sieve` | Is the Tivit + Time Sieve loop observed? Three ways of asking. |
| `python -m diagnostics.diag_tivit_combo` | Is Academy Manufactor really what makes the Deadeye loop go infinite? |
| `python -m diagnostics.run_azusa_animation_swap` | Are the §0x candidates good replacements for the LAND-ANIMATION genre? |
| `python -m diagnostics.run_azusa_draw` | Two card-draw candidates for Azusa, against the STAGED list. |
| `python -m diagnostics.run_citadel` | Bolas's Citadel in Karlov: -Swamp +Bolas's Citadel, and the life it spends. |
| `python -m diagnostics.run_combat_split` | Measures `cfg["combat_split"]` — one attack declared at the whole pod, with |
| `python -m diagnostics.run_erebos` | Erebos, Bleak-Hearted: three errors on one card. Closes queued work item 8. |
| `python -m diagnostics.run_fivedrop` | The Lorehold Penance slot, measured three ways against the same cut. |
| `python -m diagnostics.run_goldspan` | Re-measure Goldspan Dragon. Closes queued work item 0c. |
| `python -m diagnostics.run_life_costs` | What does charging a card's own life cost do? (§0i, queued item 10) |
| `python -m diagnostics.run_lorehold_pair` | The two staged Lorehold changes, measured TOGETHER. Closes queued item 0b. |
| `python -m diagnostics.run_lowstakes` | Measure the four low-stakes oracle fixes of 2026-09-05. |
| `python -m diagnostics.run_mana_colour` | What is colour-correct payment worth, and WHERE? (§0z8) |
| `python -m diagnostics.run_mingain_sweep` | Finishes the shilgengar_ult_min_gain question from run_reserve_sweep.py: |
| `python -m diagnostics.run_miracle_reducer_fix` | Ruby Medallion and Longshot, Rebel Bowman were missing from the miracle |
| `python -m diagnostics.run_reserve_sweep` | Is holding mana back for a later ability worth what it costs? |
| `python -m diagnostics.run_shilgengar_treasures` | What is queued item 13 worth? Shilgengar's Treasures, spent as mana. |
| `python -m diagnostics.run_springheart_etb` | What is queued item 16 worth? Token copies that re-trigger the host's ETB. |
| `python -m diagnostics.run_swaps_0904` | Measure the four 2026-09-04 swaps as REAL swaps, not as value-over-a-blank. |
| `python -m diagnostics.run_tivit_groups` | Group ablations for Tivit, plus a direct estimate of the table's noise floor. |

Also `./tools/regen_tables.sh`.

## Knobs

**107 knobs** are read by the engines. The registry, with defaults,
call sites and which have never been swept, is `docs/KNOBS.md`
(generated by `python -m tools.knobs --write`).

## Before you trust a number

None of these can be derived from the filesystem, so they are printed as
commands rather than as answers:

```bash
python -m tools.validate        # must print +0.00 on all 18 metrics
python -m edhmc.pending         # staged changes, and deck legality
python -m tools.check_docs      # do the docs still match the repo?
python -m tools.audit_cards     # every card against Scryfall; expect 0 ERR
```

`tools/validate.py` is the non-negotiable one: anything other than
`+0.00` means randomness is leaking between the branches of every A/B
test in the project, and no number here can be trusted until it is fixed.

