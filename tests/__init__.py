"""Tests that pin a claimed mechanism, because a docstring is not evidence.

Run from the repo root as `python -m tests.<name>`. Several carry a
`--mutate` mode that turns the fix off and asserts the test FAILS: a check
that cannot fail reads like assurance and is worse than none.
"""
