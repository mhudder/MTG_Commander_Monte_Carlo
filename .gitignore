# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Ablation caches — regenerable, and large. The finished .txt tables ARE tracked.
ablation_cache_*.json

# Spreadsheet lock/temp files
~$*.xlsx
.~lock.*

# OS
.DS_Store
Thumbs.db

# Ablation stderr progress logs — noise, the .txt tables are the artefact
ablation_*.log
